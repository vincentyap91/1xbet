/**
 * Mobile shell — header, tab bar, menu/auth sheets.
 * Depends on MobileApp core helpers attached by app.js bootstrap order:
 * load ui.js → auth.js → bet-slip.js → shell.js → app.js
 * Or single app.js (this file may be merged). Current: full app in app.js.
 */
/* Shell helpers are implemented in js/mobile/app.js for Milestone 1.
   Split further when page count grows. */
